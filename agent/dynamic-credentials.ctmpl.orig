{{ with secret "postgres/creds/dev1" }}
export POSTGRES_USERNAME="{{ .Data.username }}"
export POSTGRES_PASSWORD="{{ .Data.password }}"
spring.datasource.username={{ .Data.username }}
spring.datasource.password={{ .Data.password }}
{{ end }}
